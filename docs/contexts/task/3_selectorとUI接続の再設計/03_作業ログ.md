# 作業ログ

## 2026-04-25

### タスク開始

- task 3 を新設
- 今回の対象を `createMelodySelectors` のみに限定
- 横展開は範囲外と明記

### 方針確認

- `createXxxYyy(...)` は `RootStoreToken` ではなく、caller が持つ必要な最新値を受け取る
- selector 内で `get(store)` を行って reactive 依存を作る設計にはしない
- まずは `createMelodySelectors` を練習台にして、参照系 utility の在り方を詰める

### 現状確認

- `createMelodySelectors` は UI だけでなく business logic 側からも使われている
- そのため、今回の実態は「selector と caller 接続全体の再設計」である

### 依存棚卸しの初回確認

- `createMelodySelectors` の 4 API を確認
- 現時点では、実質的な入力は次の 3 つに絞れそうと判断
  - current score track
  - current focus range
  - current cursor
- まずはこの 3 入力を基準に、再設計案を詰める
