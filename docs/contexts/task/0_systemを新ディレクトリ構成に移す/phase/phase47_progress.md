# Phase 47 進捗

## 現在の状況
Phase 47 では `outline` と `elementRefs` を dedicated store に寄せる。

## チェックリスト
- [x] 1. `outline` と `elementRefs` の参照を洗い出す
- [x] 2. dedicated helper/session surface を追加する
- [x] 3. caller の参照を切り替える
- [x] 4. `StoreRef` から `outline` と `elementRefs` を削除する
- [x] 5. 検証を通す
