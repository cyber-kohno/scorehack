# 作業ログ

## 2026-04-22

### 実施
- タスク用フォルダを新設
- `README.md` を追加
- `01_計画.md` を追加
- `03_回帰確認チェックリスト.md` を追加

### 判断
- このタスクは大規模移設ではなく、移設後の安定化と簡素化を目的に進める
- 最初の着手対象は `StoreProps` / `StoreUtil` / `lastStore` の棚卸しとする

### 次
- `root-store` 依存整理メモを追加する
- 実際には `root-store` を使っていない helper / session から小さく整理する

## 2026-04-22 root-store依存整理

### 実施
- `StoreProps` / `StoreUtil` / `lastStore` の分布を棚卸し
- `root-store` は `Record<string, never>` の空シェルであることを再確認
- `keyboard-session.ts` と `terminal-session.ts` に、未使用の `StoreProps` 引数が残っていることを確認

### 判断
- 最初の低リスク整理対象は「引数だけ残っていて実際には `root-store` を使っていない helper」
- ここは API の意味を明確にしつつ、挙動を変えずに整理できる

### 次
- `keyboard-session.ts`
- `terminal-session.ts`
- その caller
を更新し、偽の `root-store` 依存を減らす

### 追加実施
- `keyboard-session.ts` から未使用 `StoreProps` 引数を削除
- `terminal-session.ts` から未使用 `StoreProps` 引数を削除
- terminal UI の ref setter 呼び出しを簡素化
- `cache-store.ts` の `getCache` / `setCache` を引数なし基点へ整理
- `terminal-ui-store.ts` の `getTerminalState` を引数なし基点へ整理

### 追加判断
- `root-store` の互換名残は、基点 helper から順に整理するのが安全
- 上位 API がまだ `StoreProps` を受けていても、基点が軽くなると次の段階で畳みやすい

### 次
- `StoreUtil` を受ける input router 群を分類する
- 本当に `commit` が必要なものと、単なる互換シグネチャを分ける

### 追加実施
- `StoreUtil` の分布を分類
- `bind-global-keyboard.ts` から `createStoreUtil` callback 引数を削除
- `initialize-app.ts` から `createStoreUtil` callback 引数を削除
- `App.svelte` の bootstrap 呼び出しを簡素化

### 追加判断
- `StoreUtil` はまだ input / playback orchestration では必要
- 一方で bootstrap まわりの `createStoreUtil` callback は、互換のためだけに残っていた
- まずはそのような「間接化だけの callback」を減らす方が安全

### 次
- input router 群を `commit 必須` と `lastStore 配送中心` に分ける
- そのうえで、最初に薄くできる router / action façade を選ぶ

### 追加実施
- `StoreUtil` をそのまま受け流すだけだった薄い façade を削除
  - `melody-input-router.ts`
  - `outline-input-router.ts`
  - `terminal-input-router.ts`
  - `playback-preview-router.ts`
- `keyboard-router.ts` は入力本体を直接呼ぶ形へ更新
- `outline-input.ts` は preview router を経由せず `createPlaybackActions()` を直接使う形へ更新
- `terminal-input.ts` から未使用の `getHoldCallbacks` を削除

### 追加判断
- 最初に薄くできる対象は、やはり pass-through router 群だった
- ここを外したことで、`StoreUtil` の残りは本当に orchestration がある場所へ集中した
- その中でも最初に触りやすいのは `terminal-input.ts`

### 次
- `commit` の責務を分類したメモを追加する
- 次の候補として `terminal-input.ts` を小さく整理する

### 追加実施
- `11_commit責務整理.md` を追加
- `terminal-input.ts` に `commitTerminalChange()` を追加
- helper rebuild / helper scroll / commit の組み合わせを整理

### 追加判断
- `terminal-input.ts` はやはり最初の題材としてちょうどよい
- `commit` をなくす段階ではまだないが、確定点を helper 化するだけで読みやすさはかなり上がる

### 次
- `terminal-input.ts` を基準に、`melody-input.ts` と `outline-input.ts` の commit パターンを比較する
- 次に責務整理へ入る対象を 1 つ決める

### 追加実施
- `melody-input.ts` の commit 前処理を helper 名に整理
  - `commitMelodyChange()`
  - `commitAfterPitchChange()`
  - `commitAfterHorizontalMelodyChange()`
  - `commitAfterCursorMove()`

### 追加判断
- `melody-input.ts` は `commit` 自体より「commit 前に何をそろえるか」の種類が多い
- 先にその種類へ名前を付けることで、責務分割前の理解コストを下げられる

### 次
- `outline-input.ts` にも同じ観点で commit パターンを当てる
- そのうえで、`melody` と `outline` のどちらを次の主要整理対象にするか決める

### 追加実施
- `outline-input.ts` の commit 前処理を helper 名に整理
  - `commitOutlineChange()`
  - `commitAfterOutlineScroll()`
  - `commitAfterRecalculate()`
  - `commitAfterRecalculateAndOutlineScroll()`

### 追加判断
- `outline-input.ts` は `melody-input.ts` よりも `recalculate` と `scroll` の組み合わせが中心
- helper 名にするだけで、更新の種類がかなり追いやすくなる

### 次
- `melody-input.ts` と `outline-input.ts` を比較して、次の主要整理対象を決める
- 現時点では `melody-input.ts` の方が責務分割の価値が高いかを再確認する

### 追加実施
- `melody-input.ts` から preview 音出し処理を分離
- `app/melody/melody-audition.ts` を追加
- `playSF()` の実体を `playMelodyPreviewPitch()` として移動

### 追加判断
- `melody-input.ts` の中でも preview 音出しは比較的独立していて、入力オーケストレーションと切り分けやすい
- まずこの責務を外すことで、次に `melody-input.ts` 本体を見るときに「入力制御」と「試聴ロジック」を分けて考えやすくなる

### 次
- `melody-input.ts` に残る責務を棚卸しする
- 特に `cursor操作 / note編集 / hold callback / outline同期` のどこから次に外すのが安全かを見極める

### 追加実施
- `melody-input.ts` から clipboard 操作を分離
- `app/melody/melody-clipboard-actions.ts` を追加
- `holdCtrl` にあった copy / paste ロジックを helper へ移動
- `12_melody-input責務整理.md` を追加

### 追加判断
- clipboard 操作は `holdCtrl` にまとまっていて、入力制御本体から切り離しやすい
- `preview 試聴` に続いて、`入力ではない責務` を 1 つずつ外す流れが有効
- 次の候補は `cursor / focus helper` か、独立しやすい hold subgroup のどちらか

### 次
- `melody-input.ts` の基礎操作
  - `isCursor()`
  - `getFocusNote()`
  - `moveFocus()`
  - `movePitch()`
  の扱いを見直す
- そのうえで、次にどの hold subgroup を外すのが安全か判断する

### 追加実施
- `melody-input.ts` の cursor / focus 基礎操作を helper 化
- `app/melody/melody-focus-actions.ts` を追加
- `13_melody-focus-helper整理.md` を追加

### 追加判断
- `cursor / focus` の helper 化は、次の責務整理の基準作りとして有効
- この段階ではファイルを大きく割るより、まず基本操作に名前を付ける方が安全
- 次の候補は hold subgroup の整理だが、特に pitch 系は今回の helper とつながりがよい

### 次
- `holdX / holdC` の pitch 移動系をまとめて見る
- 共通化できる部分があれば、小さく helper 化する

### 追加実施
- `holdX / holdC` の pitch 移動系を helper 化
- `app/melody/melody-pitch-actions.ts` を追加
- `14_melody-pitch-helper整理.md` を追加

### 追加判断
- `holdX` と `holdC` はどちらも pitch 移動の責務を持っており、通常移動と scale lock 移動の違いで整理できる
- ここを helper に寄せることで、`melody-input.ts` は入力分岐の読みに集中しやすくなる
- 次の候補は focus 系 subgroup か、長さ変更・横移動系 subgroup

### 次
- `holdE / holdShift` の focus 操作系をまとめて見る
- `moveMelodyFocus()` とつながる形で小さく helper 化できるか判断する

### 追加実施
- `holdE / holdShift` の focus 操作系を helper 化
- `app/melody/melody-range-focus-actions.ts` を追加
- `15_melody-focus-range-helper整理.md` を追加

### 追加判断
- focus 系は pitch 系ほど重くないが、`focusLock` の扱いを helper に寄せるだけでも分岐の見通しが良くなる
- 次に手を入れるなら、`holdF` の長さ変更か `holdD` の横移動
- 依存の軽さでは `holdF` の方が先に触りやすい

### 次
- `holdF` の長さ変更ロジックを整理する
- その後 `holdD` の横移動を、outline 同期・scroll を含めて見る

### 追加実施
- `holdF` の長さ変更ロジックを helper 化
- `app/melody/melody-length-actions.ts` を追加
- `16_melody-length-helper整理.md` を追加

### 追加判断
- `holdF` は単体 note に閉じた責務なので、`holdD` より先に整理するのが安全
- ここを外したことで、`melody-input.ts` に残る重い責務はかなり `holdD` へ集約された

### 次
- `holdD` の横移動を単体 note / range note / cursor space に分けて棚卸しする
- 最初は単体 note の横移動から helper 化を検討する

### 追加実施
- `holdD` の単体 note 横移動を helper 化
- `app/melody/melody-horizontal-actions.ts` を追加
- `17_melody-horizontal-helper整理.md` を追加

### 追加判断
- `holdD` は重いが、単体 note の横移動だけなら overlap 判定付きで独立しやすい
- この整理で `holdD` の残件は range note と cursor space にかなり絞れた

### 次
- `holdD` の range note 横移動を整理する
- その後 cursor space の移動を melody 内完結の責務として見る

### 追加実施
- `holdD` の range note 横移動を helper 化
- `melody-horizontal-actions.ts` に `moveMelodyNoteRangeHorizontally()` を追加
- `18_melody-range-horizontal-helper整理.md` を追加

### 追加判断
- range note の横移動は `holdD` の中でも最も重い部分だったが、helper 化でかなり見通しが良くなった
- この整理により、`holdD` に残る主要責務は cursor space の移動へほぼ集約された

### 次
- `holdD` の cursor space 移動を helper 化する
- その後 `melody-input.ts` 全体の残責務を再評価する

### 追加実施
- `holdD` の cursor space 移動を helper 化
- `melody-horizontal-actions.ts` に `moveMelodyCursorSpace()` を追加
- `19_melody-cursor-space-helper整理.md` を追加

### 追加判断
- これで `holdD` の主要 3 責務
  - 単体 note 横移動
  - range note 横移動
  - cursor space 移動
  を helper に寄せられた
- `melody-input.ts` はかなり入力分岐と commit 前処理中心の形に近づいた

### 次
- `melody-input.ts` 全体の残責務を再評価する
- 次に `melody-input` をさらに薄くするか、`outline-input` へ戻るかを判断する

### 追加実施
- `outline-input.ts` の arrange 判定と focus 移動を helper 化
- `app/outline/outline-input-focus-actions.ts` を追加
- `20_outline-input-focus-helper整理.md` を追加

### 追加判断
- `melody-input.ts` は一度かなり整理できたため、次の価値が高い対象を `outline-input.ts` に移した
- `outline-input.ts` はまず arrange 判定と focus 操作を外すだけでも、入力本体の見通しが良くなる

### 次
- `outline-input.ts` の element 追加 / 削除系をまとめて見る
- `recalculate + commit` のパターンを小さく helper 化できるか判断する

### 追加実施
- `outline-input.ts` の element 追加 / 削除系を helper 化
- `app/outline/outline-input-element-actions.ts` を追加
- `21_outline-input-element-helper整理.md` を追加

### 追加判断
- element 追加 / 削除系は `recalculate + commit` のパターンが揃っていて、`outline-input.ts` から切り出しやすかった
- この整理により、`outline-input.ts` の主要責務は chord data 変更系と hold callback 系へさらに寄ってきた

### 次
- `outline-input.ts` の chord data 変更系をまとめて見る
- `1`〜`7` の degree 設定と hold 系の chord 編集をどう分けるか判断する
