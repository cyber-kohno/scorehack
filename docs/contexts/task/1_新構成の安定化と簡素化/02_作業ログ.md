# 作業ログ

## 2026-04-22

### 実施
- タスク用フォルダを新設
- `README.md` を追加
- `01_計画.md` を追加
- `03_回帰確認チェックリスト.md` を追加

### 判断
- このタスクは大規模移設ではなく、移設後の安定化と簡素化を目的に進める
- 最初の着手対象は `StoreProps` / `StoreUtil` / `lastStore` の棚卸しとする

### 次
- `root-store` 依存整理メモを追加する
- 実際には `root-store` を使っていない helper / session から小さく整理する

## 2026-04-22 root-store依存整理

### 実施
- `StoreProps` / `StoreUtil` / `lastStore` の分布を棚卸し
- `root-store` は `Record<string, never>` の空シェルであることを再確認
- `keyboard-session.ts` と `terminal-session.ts` に、未使用の `StoreProps` 引数が残っていることを確認

### 判断
- 最初の低リスク整理対象は「引数だけ残っていて実際には `root-store` を使っていない helper」
- ここは API の意味を明確にしつつ、挙動を変えずに整理できる

### 次
- `keyboard-session.ts`
- `terminal-session.ts`
- その caller
を更新し、偽の `root-store` 依存を減らす

### 追加実施
- `keyboard-session.ts` から未使用 `StoreProps` 引数を削除
- `terminal-session.ts` から未使用 `StoreProps` 引数を削除
- terminal UI の ref setter 呼び出しを簡素化
- `cache-store.ts` の `getCache` / `setCache` を引数なし基点へ整理
- `terminal-ui-store.ts` の `getTerminalState` を引数なし基点へ整理

### 追加判断
- `root-store` の互換名残は、基点 helper から順に整理するのが安全
- 上位 API がまだ `StoreProps` を受けていても、基点が軽くなると次の段階で畳みやすい

### 次
- `StoreUtil` を受ける input router 群を分類する
- 本当に `commit` が必要なものと、単なる互換シグネチャを分ける

### 追加実施
- `StoreUtil` の分布を分類
- `bind-global-keyboard.ts` から `createStoreUtil` callback 引数を削除
- `initialize-app.ts` から `createStoreUtil` callback 引数を削除
- `App.svelte` の bootstrap 呼び出しを簡素化

### 追加判断
- `StoreUtil` はまだ input / playback orchestration では必要
- 一方で bootstrap まわりの `createStoreUtil` callback は、互換のためだけに残っていた
- まずはそのような「間接化だけの callback」を減らす方が安全

### 次
- input router 群を `commit 必須` と `lastStore 配送中心` に分ける
- そのうえで、最初に薄くできる router / action façade を選ぶ

### 追加実施
- `StoreUtil` をそのまま受け流すだけだった薄い façade を削除
  - `melody-input-router.ts`
  - `outline-input-router.ts`
  - `terminal-input-router.ts`
  - `playback-preview-router.ts`
- `keyboard-router.ts` は入力本体を直接呼ぶ形へ更新
- `outline-input.ts` は preview router を経由せず `createPlaybackActions()` を直接使う形へ更新
- `terminal-input.ts` から未使用の `getHoldCallbacks` を削除

### 追加判断
- 最初に薄くできる対象は、やはり pass-through router 群だった
- ここを外したことで、`StoreUtil` の残りは本当に orchestration がある場所へ集中した
- その中でも最初に触りやすいのは `terminal-input.ts`

### 次
- `commit` の責務を分類したメモを追加する
- 次の候補として `terminal-input.ts` を小さく整理する

### 追加実施
- `11_commit責務整理.md` を追加
- `terminal-input.ts` に `commitTerminalChange()` を追加
- helper rebuild / helper scroll / commit の組み合わせを整理

### 追加判断
- `terminal-input.ts` はやはり最初の題材としてちょうどよい
- `commit` をなくす段階ではまだないが、確定点を helper 化するだけで読みやすさはかなり上がる

### 次
- `terminal-input.ts` を基準に、`melody-input.ts` と `outline-input.ts` の commit パターンを比較する
- 次に責務整理へ入る対象を 1 つ決める

### 追加実施
- `melody-input.ts` の commit 前処理を helper 名に整理
  - `commitMelodyChange()`
  - `commitAfterPitchChange()`
  - `commitAfterHorizontalMelodyChange()`
  - `commitAfterCursorMove()`

### 追加判断
- `melody-input.ts` は `commit` 自体より「commit 前に何をそろえるか」の種類が多い
- 先にその種類へ名前を付けることで、責務分割前の理解コストを下げられる

### 次
- `outline-input.ts` にも同じ観点で commit パターンを当てる
- そのうえで、`melody` と `outline` のどちらを次の主要整理対象にするか決める

### 追加実施
- `outline-input.ts` の commit 前処理を helper 名に整理
  - `commitOutlineChange()`
  - `commitAfterOutlineScroll()`
  - `commitAfterRecalculate()`
  - `commitAfterRecalculateAndOutlineScroll()`

### 追加判断
- `outline-input.ts` は `melody-input.ts` よりも `recalculate` と `scroll` の組み合わせが中心
- helper 名にするだけで、更新の種類がかなり追いやすくなる

### 次
- `melody-input.ts` と `outline-input.ts` を比較して、次の主要整理対象を決める
- 現時点では `melody-input.ts` の方が責務分割の価値が高いかを再確認する

### 追加実施
- `melody-input.ts` から preview 音出し処理を分離
- `app/melody/melody-audition.ts` を追加
- `playSF()` の実体を `playMelodyPreviewPitch()` として移動

### 追加判断
- `melody-input.ts` の中でも preview 音出しは比較的独立していて、入力オーケストレーションと切り分けやすい
- まずこの責務を外すことで、次に `melody-input.ts` 本体を見るときに「入力制御」と「試聴ロジック」を分けて考えやすくなる

### 次
- `melody-input.ts` に残る責務を棚卸しする
- 特に `cursor操作 / note編集 / hold callback / outline同期` のどこから次に外すのが安全かを見極める

### 追加実施
- `melody-input.ts` から clipboard 操作を分離
- `app/melody/melody-clipboard-actions.ts` を追加
- `holdCtrl` にあった copy / paste ロジックを helper へ移動
- `12_melody-input責務整理.md` を追加

### 追加判断
- clipboard 操作は `holdCtrl` にまとまっていて、入力制御本体から切り離しやすい
- `preview 試聴` に続いて、`入力ではない責務` を 1 つずつ外す流れが有効
- 次の候補は `cursor / focus helper` か、独立しやすい hold subgroup のどちらか

### 次
- `melody-input.ts` の基礎操作
  - `isCursor()`
  - `getFocusNote()`
  - `moveFocus()`
  - `movePitch()`
  の扱いを見直す
- そのうえで、次にどの hold subgroup を外すのが安全か判断する

### 追加実施
- `melody-input.ts` の cursor / focus 基礎操作を helper 化
- `app/melody/melody-focus-actions.ts` を追加
- `13_melody-focus-helper整理.md` を追加

### 追加判断
- `cursor / focus` の helper 化は、次の責務整理の基準作りとして有効
- この段階ではファイルを大きく割るより、まず基本操作に名前を付ける方が安全
- 次の候補は hold subgroup の整理だが、特に pitch 系は今回の helper とつながりがよい

### 次
- `holdX / holdC` の pitch 移動系をまとめて見る
- 共通化できる部分があれば、小さく helper 化する

### 追加実施
- `holdX / holdC` の pitch 移動系を helper 化
- `app/melody/melody-pitch-actions.ts` を追加
- `14_melody-pitch-helper整理.md` を追加

### 追加判断
- `holdX` と `holdC` はどちらも pitch 移動の責務を持っており、通常移動と scale lock 移動の違いで整理できる
- ここを helper に寄せることで、`melody-input.ts` は入力分岐の読みに集中しやすくなる
- 次の候補は focus 系 subgroup か、長さ変更・横移動系 subgroup

### 次
- `holdE / holdShift` の focus 操作系をまとめて見る
- `moveMelodyFocus()` とつながる形で小さく helper 化できるか判断する

### 追加実施
- `holdE / holdShift` の focus 操作系を helper 化
- `app/melody/melody-range-focus-actions.ts` を追加
- `15_melody-focus-range-helper整理.md` を追加

### 追加判断
- focus 系は pitch 系ほど重くないが、`focusLock` の扱いを helper に寄せるだけでも分岐の見通しが良くなる
- 次に手を入れるなら、`holdF` の長さ変更か `holdD` の横移動
- 依存の軽さでは `holdF` の方が先に触りやすい

### 次
- `holdF` の長さ変更ロジックを整理する
- その後 `holdD` の横移動を、outline 同期・scroll を含めて見る

### 追加実施
- `holdF` の長さ変更ロジックを helper 化
- `app/melody/melody-length-actions.ts` を追加
- `16_melody-length-helper整理.md` を追加

### 追加判断
- `holdF` は単体 note に閉じた責務なので、`holdD` より先に整理するのが安全
- ここを外したことで、`melody-input.ts` に残る重い責務はかなり `holdD` へ集約された

### 次
- `holdD` の横移動を単体 note / range note / cursor space に分けて棚卸しする
- 最初は単体 note の横移動から helper 化を検討する

### 追加実施
- `holdD` の単体 note 横移動を helper 化
- `app/melody/melody-horizontal-actions.ts` を追加
- `17_melody-horizontal-helper整理.md` を追加

### 追加判断
- `holdD` は重いが、単体 note の横移動だけなら overlap 判定付きで独立しやすい
- この整理で `holdD` の残件は range note と cursor space にかなり絞れた

### 次
- `holdD` の range note 横移動を整理する
- その後 cursor space の移動を melody 内完結の責務として見る

### 追加実施
- `holdD` の range note 横移動を helper 化
- `melody-horizontal-actions.ts` に `moveMelodyNoteRangeHorizontally()` を追加
- `18_melody-range-horizontal-helper整理.md` を追加

### 追加判断
- range note の横移動は `holdD` の中でも最も重い部分だったが、helper 化でかなり見通しが良くなった
- この整理により、`holdD` に残る主要責務は cursor space の移動へほぼ集約された

### 次
- `holdD` の cursor space 移動を helper 化する
- その後 `melody-input.ts` 全体の残責務を再評価する

### 追加実施
- `holdD` の cursor space 移動を helper 化
- `melody-horizontal-actions.ts` に `moveMelodyCursorSpace()` を追加
- `19_melody-cursor-space-helper整理.md` を追加

### 追加判断
- これで `holdD` の主要 3 責務
  - 単体 note 横移動
  - range note 横移動
  - cursor space 移動
  を helper に寄せられた
- `melody-input.ts` はかなり入力分岐と commit 前処理中心の形に近づいた

### 次
- `melody-input.ts` 全体の残責務を再評価する
- 次に `melody-input` をさらに薄くするか、`outline-input` へ戻るかを判断する

### 追加実施
- `outline-input.ts` の arrange 判定と focus 移動を helper 化
- `app/outline/outline-input-focus-actions.ts` を追加
- `20_outline-input-focus-helper整理.md` を追加

### 追加判断
- `melody-input.ts` は一度かなり整理できたため、次の価値が高い対象を `outline-input.ts` に移した
- `outline-input.ts` はまず arrange 判定と focus 操作を外すだけでも、入力本体の見通しが良くなる

### 次
- `outline-input.ts` の element 追加 / 削除系をまとめて見る
- `recalculate + commit` のパターンを小さく helper 化できるか判断する

### 追加実施
- `outline-input.ts` の element 追加 / 削除系を helper 化
- `app/outline/outline-input-element-actions.ts` を追加
- `21_outline-input-element-helper整理.md` を追加

### 追加判断
- element 追加 / 削除系は `recalculate + commit` のパターンが揃っていて、`outline-input.ts` から切り出しやすかった
- この整理により、`outline-input.ts` の主要責務は chord data 変更系と hold callback 系へさらに寄ってきた

### 次
- `outline-input.ts` の chord data 変更系をまとめて見る
- `1`〜`7` の degree 設定と hold 系の chord 編集をどう分けるか判断する

### 追加実施
- `outline-input.ts` の chord data 変更系を helper 化
- `app/outline/outline-input-chord-actions.ts` を追加
- `22_outline-input-chord-helper整理.md` を追加

### 追加判断
- chord data 変更系は `control` と hold callback にまたがっていたが、変更内容そのものは helper へ寄せやすかった
- この整理により、`outline-input.ts` はかなり入力分岐と `recalculate / commit` パターン中心の形に近づいた

### 次
- `outline-input.ts` 全体の残責務を一度再評価する
- さらに薄くするか、別の論点へ進むかを判断する

### 追加実施
- `melody-input.ts` / `outline-input.ts` の現状を再評価
- `preview-util.ts` に残っていた古い import パス
  - `../../store/store`
  を
  - `../../state/root-store`
  に修正
- `23_input再評価と次候補.md` を追加

### 追加判断
- `melody-input.ts` と `outline-input.ts` は、保守性の観点でかなり良い地点まで来ている
- これ以上 helper を増やすより、次は
  - `StoreProps`
  - `StoreUtil`
  - `lastStore`
  のような root-store 由来の互換命名を見直す価値が高い

### 次
- `StoreProps / StoreUtil / lastStore` の用途別棚卸しに戻る
- 最初の小さな命名整理対象を 1 つ選ぶ

### 追加実施
- `StoreProps` / `StoreUtil` の役割が読みやすい別名として
  - `RootStoreToken`
  - `CommitContext`
  を `root-store.ts` に追加
- `state/project-data/*` の token-only API を `RootStoreToken` ベースへ整理
- `app/cache/cache-actions.ts` も `RootStoreToken` ベースへ整理
- `24_root-store命名再整理.md` を追加

### 追加判断
- `StoreProps` をいきなり消すより、まず「これは空の root store token です」と読める別名を足す方が安全
- 最初の適用先としては、`project-data` のような token-only 領域が最も低リスク
- `StoreUtil` はまだ orchestration の意味が強いので、先に `CommitContext` を別名として置いて役割を明確にする段階に留める

### 次
- `RootStoreToken` を token-only helper / selector に少しずつ広げる
- その後、`StoreUtil` が残る場所を `CommitContext` 観点で見直す

### 追加実施
- `shell-ui-store.ts` の token-only selector を `RootStoreToken` ベースへ整理
- `arrange-state.ts` の token-only helper を `RootStoreToken` ベースへ整理

### 追加判断
- `shell-ui-store` は dedicated state を読むだけなので、token-only 代表例として扱いやすい
- `arrange-state` も orchestration ではなく selector / helper の性格が強く、同じ観点で整理できる

### 次
- `RootStoreToken` を他の token-only selector / helper に広げる
- 特に `timeline-ui-store` / `outline-ui-store` を、どこまで安全に寄せられるか確認する

### 追加実施
- `timeline-ui-store.ts` を `RootStoreToken` ベースへ整理
- `outline-ui-store.ts` を `RootStoreToken` ベースへ整理

### 追加判断
- `timeline-ui-store` と `outline-ui-store` は cache / project-data helper に token を流す形だが、root store 実体は読んでいない
- そのため、`StoreProps` より `RootStoreToken` の方が役割を正しく表せる

### 次
- token-only 領域で `StoreProps` が残る場所をもう一段洗い出す
- その後、`StoreUtil` / `CommitContext` 側の整理に戻る

### 追加実施
- `apply-layout-variables.ts` を `RootStoreToken` ベースへ整理
- 薄い action façade を `RootStoreToken` ベースへ整理
  - `melody-actions.ts`
  - `outline-actions.ts`
  - `terminal-actions.ts`
  - `project-data-actions.ts`

### 追加判断
- façade 層は「挙動を持つ本体」ではなく token を下位へ流す役目が中心
- この層を先に整理することで、`StoreProps` が残る場所の意味がかなり見えやすくなった

### 次
- `StoreProps` が残る場所を token-only / reducer・scroll / project-io / playback に分け直す
- その後、`CommitContext` 側へ戻って次の小さい対象を選ぶ

### 追加実施
- `StoreProps` / `StoreUtil` の残件を用途別に再分類
- `25_root-store残件分類.md` を追加
- `CommitContext` が自然な小さい orchestrator を先に整理
  - `terminal-input.ts`
  - `playback-actions.ts`
  - `initialize-app.ts`

### 追加判断
- `StoreUtil` はもう「本当に commit を持つ場所」にかなり寄っている
- そのため次の段階では、token-only 側の整理と並行して、意味が明確な orchestrator から `CommitContext` へ寄せるのが安全
- `keyboard-router` や `melody-input` のような中心 orchestrator は、まだ周辺影響が大きいので一段あとがよい

### 次
- token-only な `StoreProps` 残件をもう一段減らす
- その後、`CommitContext` を `terminal-input` 以外の小さい orchestrator へ広げる

### 追加実施
- token-only な cache / scroll helper を `RootStoreToken` ベースへ整理
  - `cache-store.ts`
  - `get-timeline-focus-pos.ts`
  - `scroll-actions.ts`
  - `piano-editor-scroll.ts`

### 追加判断
- `cache-store` と scroll helper は、token-only 領域の中でも典型的で、命名整理の効果が分かりやすい
- この整理により、残る `StoreProps` は reducer / project-io / playback / shell 本体にさらに寄った

### 次
- token-only 残件をもう一段見直して、`StoreProps` が本当に必要な場所をさらに絞る
- その後 `CommitContext` を次の orchestrator へ広げる

### 追加実施
- `project-io` の token-only 入口を `RootStoreToken` ベースへ整理
  - `project-io-service.ts`
  - `load-project.ts`
  - `save-project.ts`

### 追加判断
- `project-io` は副作用境界ではあるが、`StoreProps` の役割としては root state 本体ではなく token 配送に近い
- そのため、`RootStoreToken` へ寄せても意味が崩れず、むしろ読みやすくなった

### 次
- token-only 残件をさらに見直す
- reducer / playback / shell 本体のような「本当に意味のある `StoreProps` / `CommitContext`」だけを残す方向へ進める

### 追加実施
- 小さい token-only 残件を `RootStoreToken` ベースへ整理
  - `terminal-ui-store.ts`
  - `melody-audition.ts`
  - `melody-cursor-state.ts`
  - `root-control.ts`

### 追加判断
- `void lastStore` が残っている場所は、いま見直す価値が高い
- この整理により、`StoreProps` の残件はかなり reducer / cache / playback / shell 本体へ寄ってきた

### 次
- token-only 残件がまだ残るかを確認する
- その後、`CommitContext` を次の orchestrator に広げる

### 追加実施
- `CommitContext` を次の orchestrator に適用
  - `preview-util.ts`
  - `keyboard-router.ts`

### 追加判断
- 今回は責務分割ではなく、`commit を持つ文脈` の名前を合わせることを優先
- `keyboard-router` は中心寄りだが、型名と引数名の置換に留めれば安全に進められる

### 次
- `StoreProps` の token-only 残件が本当に残っているかを確認する
- その後、`CommitContext` を `arrange input` や `terminal builder` に広げるか判断する

### 追加実施
- `CommitContext` を `arrange input` 側へ適用
  - `arrange-input.ts`
  - `piano-editor-input.ts`
  - `guitar-editor-input.ts`
  - `finder-input.ts`

### 追加判断
- `arrange input` は `keyboard-router` の次の受け口だが、`melody-input` ほど中心ではない
- そのため、責務分割より先に `commit を持つ文脈` の名前を揃える対象として扱いやすい

### 次
- `StoreProps` の token-only 残件が本当に残っているかを最後に確認する
- その後、`CommitContext` を `terminal builder` へ広げるか判断する

# 2026-04-23 root-store 命名整理の締め
- `preview-util.ts` を HEAD の健全な内容から再構成し、`RootStoreToken / CommitContext` 置換を載せ直した
- コメントを保ったまま `npm run check / build / cargo check` を再度通した
- `melody-reducer.ts` と `outline-reducer.ts` を `RootStoreToken` ベースに変更した
- `root-store.ts` から `StoreProps` を削除し、`RootStoreToken = Record<string, never>` に統一した

# 2026-04-23 lastStore ローカル名整理
- `initialize-app.ts`
- `playback-actions.ts`
- `terminal-input.ts`
- `keyboard-router.ts`

上の小さい orchestrator で、`CommitContext` から取り出した後のローカル名を `rootStoreToken` に変更した。
フィールド名自体はまだ変えず、まずは読みやすさを上げる方針にしている。

# 2026-04-23 commit の位置づけ明文化
- `root-store.ts` の `commit()` に、互換同期バリアであることを示すコメントを追加した
- task docs にも、現状の commit は selective update ではなく安全側の同期点であることを追記した

# 2026-04-23 orchestrator 側の lastStore 別名整理
- `preview-util.ts`
- `melody-input.ts`
- `outline-input.ts`
- `arrange/piano-editor-input.ts`
- `arrange/finder/finder-input.ts`

上の orchestrator でも `CommitContext` から取り出したローカル名を `rootStoreToken` に揃えた。
これで主要な利用箇所では、`lastStore` という古い語感をローカルではほぼ持ち込まない形になった。

# 2026-04-23 型の置き場ルールの初回整理
- `26_型の置き場ルール.md` を追加した
- `project-file-store.ts` を小さい適用例として平らにし、store 専用 state 型は store ファイルに同居させる方針を明文化した

# 2026-04-23 melody-store の初回評価
- `27_melody-store評価.md` を追加した
- `melody-store.ts` は value 集約として当面残すが、type-only import は `melody-types.ts` へ直接寄せてよい方針を明文化した
- 小さい適用例として `import-audio.ts`、`project-io-service.ts`、`melody-range-focus-actions.ts` を直接型参照に変更した

# 2026-04-23 melody-store type-only 直接参照の追加
- `melody-audition.ts`
- `melody-focus-actions.ts`
- `MelodyUnitDisplay.svelte`
- `Factors.svelte`

type-only な `StoreMelody.*` を追加で `melody-types.ts` へ直接寄せた。
value 利用はそのまま façade 経由に残している。

# 2026-04-23 arrange-store 系の初回評価
- `28_arrange-store評価.md` を追加した
- `arrange-store.ts` / `piano-editor-store.ts` / `piano-backing-store.ts` は、当面残す方針を整理した
- 小さい整理として `piano-backing-store.ts` の `MelodyNorm` 直接参照化と、`piano-editor-store.ts` の未使用 import 削除を行った

# 2026-04-23 lastStore 未置換の止血
- `melody-input.ts`
- `outline-input.ts`
- `arrange/piano-editor-input.ts`
- `arrange/finder/finder-input.ts`

`lastStore` の未置換が残っていたため、`rootStoreToken` に修正した。
現在 `rg` で残る `lastStore` は、`CommitContext` からの別名取り出しか reducer 内の意図的なローカル別名のみ。

# 2026-04-23 melody-pitch-actions の Tonality 修正
- `melody-pitch-actions.ts`

`getNextScalePitch()` と scale lock pitch 移動 helper の `tonality` 引数が `number[]` になっていたため、
`MusicTheory.Tonality` に修正した。
`melody-input.ts` 側はもともと `scoreBase.tonality` を渡しており、呼び出し側との型整合も取れている。

# 2026-04-23 reducer / actions / lastStore 論点の整理
- `29_reducer_actions_laststore整理.md` を追加した

`reducer` が
- mutation
- selector/accessor
- orchestration

を混在して持っている点を整理した。
今後は、更新専用 helper から `RootStoreToken` を外し、読み取り系と orchestration を別レイヤへ寄せる方向を本筋にする。
