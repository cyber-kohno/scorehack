# タスク1 区切りメモ

## 状態
- `system` の新構成移行は完了
- 新構成の安定化と簡素化も、初期テーマについては一区切りついた

## このタスクで整理できたこと
- `RootStoreToken / CommitContext` への命名整理
- `commit()` の役割の明文化
- 型の置き場ルールの初回整理
- `melody-store` / `arrange-store` 系の扱いの初回評価
- `input` / `helper` / `root-store` 周辺の初期的な簡素化

## 次タスクへ送る論点
- 旧 `Reducer` のまとまりをどう分離するか
- `actions` / `reducer` / `selector` / `orchestration` の責務をどう整理するか
- 更新専用 helper から `RootStoreToken` を外していけるか

## 次
- `2_reducer_actions_laststore整理` に移る
